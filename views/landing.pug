extends layout

block content
    h1= title
    p Welcome to #{title}
    form(action="/", method="POST")
        input(name="usernameInput", type="username", placeholder="Enter a username")
        button(type="submit") Submit
